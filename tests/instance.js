import ServerPushWebpackPlugin from '../'

describe('create request by default', () => {
  describe('checking Request', () => {
    it('initialization', () => {
      const options = {
        target: 'nginx',
      }
      const plugin = new ServerPushWebpackPlugin(options)

    })

  })

})
